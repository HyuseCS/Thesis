# Literature Matrix

Columns follow the axis of [[GOAL]] v0.2: who is optimized (autonomous agent or human driver), at what track granularity, and on what evidence.

| Citation Key | Subject | Method | Track granularity | Data / scenario scale | Key reported result | Gap for this thesis |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| [[Hojaji2023_TelemetryML]] | Human drivers | k-means performance levels + XGBoost classification of telemetry; 84 → 46 → 10 features (`Sec. 3.2`) | Whole lap only | 782 laps after cleaning, ACC, Brands Hatch, pooled unknown drivers (`Sec. 2.1`, `Table 1`) | Top features: speed, RPM, g_lat, throttle, steer, lane deviation (`Fig. 3`); "absolute accuracy of 92.2%" on one 70/30 split (`Sec. 3.2`) | Authors defer segment-level estimation: *"focus on a specific segment rather than the data for the full lap … We defer this work as the future work."* (`Sec. 3.2`) — this thesis' question |
| [[Hojaji2024_DrivingBehaviour]] | Human drivers (93 lab participants) | k-means (k = 2, Elbow + Silhouette) on lap time into FAST/SLOW, then group-averaged telemetry traces plus derived metrics: lane deviation, understeer/oversteer, trail braking, throttle release (`Sec. 2.2`, `Sec. 3.1`) | Sections S1–S9 / T1–T9 drawn on plots only. No per-sector metric computed (`Figs. 2–3`) | 557 laps after cleaning (571 → z-score), 93 participants, ACC, Brands Hatch, car not reported, Logitech Pro rig, 50 Hz (`Sec. 2.1`, `Sec. 2.2`) | FAST 441 laps, mean 89.93 s vs SLOW 115 laps, mean 102.86 s (`Table 1`); undefined "accuracy of 81.42%" (`Sec. 3.1`); FAST/SLOW differences are largest in the first three corners (T2 hairpin) and near zero in the last three (`Sec. 3.2`) | Per-corner differences are only eyeballed from averaged traces. No sector times, no statistics, no feature importance per sector. Corner carry-over (T2 → T3) is described but not measured — this is the thesis' sector-level question |
| [[Hojaji2026_TelemetryEA]] | Human drivers (the EA output is a synthetic KPI vector that no one drove) | k-means (k = 3) performance levels + Pearson filter + XGBoost importance (top 10 KPIs); EA (pop 120, 200 gens, tournament 3, 2-pt crossover 0.8, Gaussian mutation 0.15/σ 0.05, elitism 2) with a hand-set linear surrogate fitness T + 0.1·P, sector times clamped to 90–105 % of baseline (`Sec. 3.4`, `Sec. 3.5.2–3.5.3`) | 20 sectors (10 corner-, 10 straight-dominant) in the chromosome, but feature importance is **global** (`Sec. 3.5.1`, `Sec. 5`) | 135 drivers, 1,180 → 865 laps / 17,279 segments; ACC v1.9, McLaren 720S GT3, Laguna Seca, 50 Hz, 74 ch (`Sec. 3.1–3.2`); EA runtime and hardware not reported | XGBoost Acc 0.87, F1 0.85, AUC 0.90 (70/30 split + 10-fold CV, class balance not reported, `Sec. 3.4`); predicted EA lap "approximately 81-82 s" vs 91.05 s baseline and 86 s human minimum (`Sec. 4.2`) — surrogate prediction, never driven | Admitted: *"feature importance in this study was computed globally rather than at the individual sector level … Future work could therefore explore segment-level models"* (`Sec. 5`). Sector times are summed independently, so there is no corner interdependency. The 81–82 s result sits at the 90 % clamp floor (0.9 × 91.05 = 81.945 s, our arithmetic) |
| [[Scheffe2022_SCR]] | Autonomous vehicle | Sequential Convex Restriction, RTI-MPC, single-track + Pacejka; overlapping convex polygons | Polygon set over the whole track (1090 tessellated → 178 merged, `Fig. 3`) | 1:43 scale track ~250 m, MATLAB + CPLEX 12.10, AMD Ryzen 5 3600 (`Sec. VI`) | Lap 10.1 s vs SL 11.0 s (`Table I`); ~73 ms median solve vs SL ~35 ms (`Fig. 8`); feasibility guaranteed (`Thm. 2`) | Optimizes a controller, not a driver. No human telemetry, no per-corner attribution, output is not coaching advice |
| [[Scheffe2022_SCR]] (baseline) | Autonomous vehicle | Sequential Linearization: relaxation + slack + trust region | Tangent-line constraints per step | same as above | ~35 ms median solve (`Fig. 8`); can leave the track (`Fig. 6`) | same |
| [[Evans2023_TAL]] | Autonomous agent (F1Tenth DRL) | Trajectory-aided learning: TD3 reward shaped by a min-curvature + pure-pursuit classical planner (`Sec. III-C`, `Eq. 3`) | None — no track segmentation; track limits enforced only by a −1 crash reward (`Sec. III-C`) | 4 maps, F1Tenth simulator, 10 Hz planning / 100 Hz dynamics; hardware and training time not reported in the source (`Sec. IV-A`) | Completion >75 % on all four maps at 6 m/s (`Sec. IV-C`); 60 % at 6 m/s and 40 % at 8 m/s on ESP (`Sec. IV-B`) | Simulation only, transfer untested (`Sec. V`); no per-corner attribution and no human-actionable output; performance bounded by its teacher trajectory |
| [[Garlick2021_MLTrajectory]] | Autonomous vehicle (no human telemetry) | Feed-forward ANN (450/200/200, Huber + Nadam) predicting racing-line waypoints; labels from TUMFTM optimal-control solver (`Sec. 2.4.1`, `Sec. 2.1`) | Per-waypoint along track normals; no sector or corner segmentation | >6000 generated laps, ~2 weeks single-thread label generation; 288 test circuits; inference ~33 ms on Intel Core i9-9980HK (`Sec. 2.1`, `Sec. 3.2`, `Table 2`) | Path error vs the optimal line: MAE 0.267 m, RMS 0.376 m, apex 0.111 m (`Table 3`) | **Degrades at consecutive interacting corners** (`Sec. 3.1`) — the interdependency this thesis measures. No speed trace and no lap time ever computed (`Sec. 5`); output is a line, not driver-actionable advice |
| [[Ghignone2023_TCDriver]] | Autonomous agent (1:10 F1TENTH car; no human driver) | SAC policy conditioned on a planner trajectory (20 points at 20 cm in the car frame, obs dim 56, MLP 56/256/256/2); tire-friction domain randomization (`Sec. 2.2.2`, `Sec. 2.3`) | None — continuous Frenet progress and a rolling 4 m window; all results per lap (`Sec. 2.2.2`, `Tables 2–5`) | Online rollouts, no dataset; 5e5 training steps, 200 eval runs/config, 10+10 real-car runs; tracks 89–470 m; inference on i7-10700K (`Sec. 3`) | Crash ratio 2.50 % vs end-to-end 73.50 % and MPC 80.50 % under tire mismatch; lap 10.798 s vs MPC 10.094 s (`Table 2`); inference 0.27 ms vs MPC 11.2 ms (`Table 4`) | Black-box policy weights, no feature importance and no attribution; admitted "bang-bang control" signature (`Sec. 4`). No sector segmentation, no corner-interdependency analysis |
| [[Samak2021_HybridImitationRL]] | Autonomous agent; 10 human players used only as a benchmark to beat (`Sec. III-C`) | Behavioral cloning + GAIL + curiosity + PPO on a FCNN 2×(128, Swish); throttle fixed at 20 % (`Sec. III-B`, `Table I`) | 14 virtual checkpoints A–N used for reward shaping, not analysis; no per-sector times reported (`Fig. 2`, `Sec. III-B`) | 11 demo laps + 5e6 steps; 10 autonomous vs 100 manual laps by 10 players; 1 track, 1 car; i7-8750H + RTX 2070, 19 h 39 min training (`Sec. III-C`, `Sec. IV-C`) | Agent mean 63.148 s vs human mean 64.62 s, wins 9/10 laps (`Fig. 5`, `Sec. IV-B`); deployment latency 3.42 ms/cycle (`Sec. IV-C`) | Output is policy weights, not advice — the winning move is "nearly unachievable by humans" and the win is conceded to be "partly by its higher control frequency" (`Sec. IV-B`, `Sec. V`). Corner interdependency observed but never measured (`Sec. IV-A`) |
| Kabzan et al. 2019 *(PDF not in corpus)* | Autonomous vehicle | Learning-based MPC | — | — | — | Cited by the base document; needs ingestion |
| Lee et al. 2025 *(PDF not in corpus)* | Autonomous agent | Vision-based RL, Gran Turismo 7 | — | — | — | Cited by the base document; needs ingestion |
